{
  "provider": "ollama",
  "api_base": "http://ollama:11434/v1",
  "api_key": "ollama",
  "model_name": "Godmoded/llama3-lexi-uncensored",
  "notes": [
    "This project uses a single Ollama host endpoint for the main web assistant and any optional sandbox-side AI helpers.",
    "The Windows sandbox does not run a separate LLM stack by default.",
    "Keep this file aligned with docker-compose environment variables when changing models."
  ]
}
